export const metadata = {
    title: "Counties"
}

export default function Counties() {
    return (
        <div>
            <h1>
                Counties Page
            </h1>
        </div>
    )
}